# --- Core (required for mock mode + web UI; all pure-python/wheels) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
pyyaml==6.0.2

# --- Live capture (needed once hardware is attached) ---
# BLE scanning via the SENA UD-100 (BlueZ). Pure python.
bleak==0.22.3

# --- Camera + ALPR (needed once a webcam is attached) ---
# opencv-python-headless is lighter for a server node; swap for opencv-python if you
# want local imshow debugging.
opencv-python-headless
numpy
# ALPR: YOLO plate detector + CCT OCR. onnxruntime is a runtime dep that fast-alpr does
# not always pull in automatically — install it explicitly.
fast-alpr
onnxruntime

# Pillow: renders synthetic capture images in mock mode (real webcam frames come from
# opencv). Safe to install; harmless once the camera is live.
Pillow==12.3.0

# Note: TPMS decoding uses the `rtl_433` *system binary* (apt install rtl-433),
# not a python package.
